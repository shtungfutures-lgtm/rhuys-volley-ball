[build]
  command = "echo 'Static RHUYS site'"
  publish = "."

[build.environment]
  NETLIFY_NEXT_PLUGIN_SKIP = "true"

[functions]
  directory = "netlify/functions"

[dev]
  framework = "#static"
  publish = "."
